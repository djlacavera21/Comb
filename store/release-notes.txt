v0.8 adds a narrowly scoped Magento Open Source / Adobe Commerce checkout adapter, a searchable local-only community coupon catalog for installed signature-verified feeds, and a permission-equivalent Firefox 128+ desktop runtime path. Required Firefox CI now builds and temporary-installs the exact runtime ZIP, verifies startup without a pre-granted feed origin, preserves real prompt denial, rejects an approved tampered envelope while rolling back its new grant, installs the valid in-memory feed through a cookie-free/referrer-free synthetic HTTPS retry, and proves source removal clears the production 720-minute alarm and unused origin grant. Firefox remains unsigned until that hosted real-Firefox gate passes and Mozilla review/signing completes. The strict machine-readable synthetic fixture matrix covers localized Magento contracts, while the fail-closed GitHub publication workflow blocks version, tag, commit, and checksum drift. The safe compatibility report remains local-only and never uploaded. Creator attribution remains release-blocking: existing affiliate URL tags and attribution cookies must remain unchanged after full real-Chrome and real-Firefox checkout runs.
