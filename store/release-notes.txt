v0.7 adds a strict machine-readable synthetic fixture matrix, commit-pinned independent-review intake, and a manually authorized fail-closed GitHub publication workflow. Every checkout fixture now carries exact adapter/version/state/click expectations, while tests reject live hosts, missing coverage, version drift, tag reuse, checksum drift, and publication from anything other than the supplied current main SHA. The safe compatibility report remains local-only and never uploaded. Creator attribution remains release-blocking: existing affiliate URL tags and attribution cookies must remain unchanged after a full real-Chrome coupon run.
