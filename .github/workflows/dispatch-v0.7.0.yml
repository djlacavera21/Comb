name: Retired Comb v0.7.0 release dispatcher

# Audit receipt only. The one-shot dispatcher succeeded in run 31853782756 and
# started guarded release run 31853787164 for v0.7.0 at the authorized commit.
# There is intentionally no push trigger and no token write permission.
on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired dispatcher
        run: |
          echo "This one-shot dispatcher is retired."
          echo "Comb v0.7.0 was published by guarded release run 31853787164."
          exit 1
